apiVersion: "k8s.cni.cncf.io/v1"
kind: NetworkAttachmentDefinition
metadata:
  name: host-device-capture-conf
spec:
  config: |
    {
        "cniVersion": "0.3.1",
        "type": "host-device",
        "device": "capture0"
    }
